
#""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
# Mod CtrlP
#""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""""
#bind C-p new-window -n "Select" "tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse | xargs tmux switch-client -t"
#bind -n M-p popup "tmux list-sessions | sed -E 's/:.*$//' | grep -v \"^$(tmux display-message -p '#S')\$\" | fzf --reverse | xargs tmux switch-client -t"


# bind -n M-p popup -w 40% -h 40% "\
#     tmux list-sessions -F '#{session_name}' |\
#     sed '/^$/d' |\
#     fzf --reverse |\
#     xargs tmux switch-client -t"


# bind -n M-o popup -E -w 40% -h 40% "\
#     tmux list-windows -F '#{window_name}' |\
#     sed '/^$/d' |\
#     fzf --reverse  |\
#     xargs tmux select-window -t"
