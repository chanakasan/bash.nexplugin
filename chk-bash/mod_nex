_() {
  local v1="${1:-_nil_}"
  local v2="$2"
  if [ "$v1" = "dotfiles" ]; then
    cd $HOME/dotfiles
  elif [ "$v1" = "sites" ]; then
    cd $HOME/sites
  elif [ "$v1" = "temp" ]; then
    cd $HOME/temp
  else
    _nex_$v1
  fi
}

