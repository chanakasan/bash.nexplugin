#!/usr/bin/env bash

set -e

main() {
  local a="$1"
  local example=$chk_dotfiles_path/mux-bin/mux-example
  if [ -z "$a" ]; then
    mux-home
  elif [ "$a" = "ex" ]; then
    vi $example
  else
    mux-$a
  fi
}

main "$@"
